#include <iostream>

int main() {
    //:: scope resolution operator which tells the compiler
    //which namespace the object cout is from in this case std
    std::cout << "Hello, Object-Oriented Programming!" << std::endl;
    return 0;
}